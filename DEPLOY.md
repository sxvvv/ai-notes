# 🚀 部署指南

## 📋 部署前准备

✅ 确保项目已构建完成（`dist` 目录已生成）
✅ 配置文件已正确设置
✅ Supabase 数据库已配置

## 🎯 方法一：Vercel 部署（推荐）

### 步骤 1：安装 Vercel CLI

```bash
npm install -g vercel
```

### 步骤 2：登录 Vercel

```bash
vercel login
```

会打开浏览器，用 GitHub/Email/Google 登录

### 步骤 3：部署项目

```bash
cd ai-notes
vercel --prod
```

首次部署会询问：
- **Set up and deploy?** → 输入 `Y`
- **Which scope?** → 选择你的账户
- **Link to existing project?** → 输入 `N`（首次部署）或 `Y`（已有项目）
- **Project name?** → 输入 `ai-notes` 或自定义名称
- **Directory?** → 直接回车（使用当前目录）
- **Override settings?** → 输入 `N`

### 步骤 4：获取部署地址

部署完成后会显示类似：
```
✅ Production: https://ai-notes-xxxxx.vercel.app
```

### 步骤 5：自定义域名（可选）

1. 访问 https://vercel.com/dashboard
2. 选择你的项目
3. Settings → Domains
4. 添加你的自定义域名

## 🌐 方法二：Netlify 部署

### 步骤 1：安装 Netlify CLI

```bash
npm install -g netlify-cli
```

### 步骤 2：登录

```bash
netlify login
```

### 步骤 3：初始化并部署

```bash
cd ai-notes
netlify init
```

会询问：
- **Create & configure a new site** → 选择 Yes
- **Team** → 选择你的团队
- **Site name** → 输入 `ai-notes` 或自定义名称
- **Build command** → 输入 `npm run build`
- **Directory to deploy** → 输入 `dist`

### 步骤 4：生产部署

```bash
netlify deploy --prod
```

### 步骤 5：获取地址

会显示类似：
```
✅ Website URL: https://ai-notes.netlify.app
```

## 📦 方法三：通过网页部署（无需命令行）

### Vercel 网页部署：

1. **访问** https://vercel.com
2. **点击** "Add New Project" 或 "Import Project"
3. **如果项目在 GitHub**：
   - 选择你的仓库
   - 配置：
     - **Framework Preset**: Vite
     - **Root Directory**: `ai-notes`
     - **Build Command**: `npm run build`
     - **Output Directory**: `dist`
   - 点击 **Deploy**

### Netlify 网页部署：

1. **访问** https://app.netlify.com/drop
2. **拖拽** `dist` 文件夹到页面
3. **等待** 几秒钟，会显示你的网站地址

或：

1. **访问** https://app.netlify.com
2. **点击** "Add new site" → "Import an existing project"
3. **连接** GitHub 仓库
4. **配置**：
   - Base directory: `ai-notes`
   - Build command: `npm run build`
   - Publish directory: `dist`
5. **点击** Deploy

## 🔄 更新部署

部署后，如果需要更新：

### Vercel：

```bash
npm run build
vercel --prod
```

### Netlify：

```bash
npm run build
netlify deploy --prod --dir=dist
```

### 自动部署（推荐）：

如果连接了 GitHub，每次 push 代码会自动部署！

## ✅ 部署后检查

部署完成后，访问你的地址，检查：

- [ ] ✅ 页面正常加载
- [ ] ✅ 仪表板显示正常
- [ ] ✅ 笔记列表可以加载
- [ ] ✅ 编辑器功能正常
- [ ] ✅ AI 助手功能正常
- [ ] ✅ 搜索功能正常
- [ ] ✅ 移动端响应式正常
- [ ] ✅ Supabase 连接正常
- [ ] ✅ 权限控制功能正常

## 🔧 环境变量配置

如果需要在 Vercel 中配置环境变量：

1. 访问 Vercel Dashboard
2. 选择项目 → Settings → Environment Variables
3. 添加需要的环境变量

## 📝 常见问题

### 问题：部署后无法访问

**解决：**
- 检查构建是否成功
- 查看 Vercel/Netlify 的部署日志
- 确认 Supabase 配置正确

### 问题：Supabase 连接失败

**解决：**
- 检查 Supabase URL 和 API Key 是否正确
- 确认 Supabase 项目的 RLS 策略配置
- 检查网络连接

### 问题：AI 功能不工作

**解决：**
- 检查 AI API 配置是否正确
- 查看浏览器控制台错误信息
- 确认 API Key 有效

## 🎯 推荐方案

**最佳选择：Vercel**
- ✅ 免费
- ✅ 自动 HTTPS
- ✅ 全球 CDN
- ✅ 自动部署（连接 GitHub）
- ✅ 零配置
- ✅ 更好的域名格式

---

**🚀 开始部署你的应用吧！**
